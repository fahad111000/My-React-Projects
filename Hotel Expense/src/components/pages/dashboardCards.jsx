import { Box, Heading } from '@chakra-ui/react'

export default function DashboardCards() {
    return (
        <Box p={2} px={4}>
            <Heading py={1} color={'blue.500'}>Cards!</Heading>
        </Box>
    );
}